/**
 * An example function in JavaScript.
 * @param {Number} num1 The first number.
 * @param {Number} num2 The second number. Defaults to 5.
 * @returns {Boolean} The returned value.
 */
function exampleFunction(num1, num2 = 5) {
    let result = num1 + num2;
    return true;
}

/**
 * Does not do anything.
 * @returns {String} The returned string.
 */
function dummyFunction() {
    return 'False';
}
